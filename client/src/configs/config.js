const config = {
    BASE_URL: "", // For development leave this option empty, for production set your ngrok link
    DEVELOPMENT: false, // Set true if you want enable state manager logger
};

export default config;
